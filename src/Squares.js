import React from "react";
import { Typography } from "@material-ui/core";

export const Squares = () => {
  return (
    <div className="headerTitle">
      <Typography variant="h1" gutterBottom>
        This is a Squares page.
      </Typography>
    </div>
  );
};

export default Squares;
